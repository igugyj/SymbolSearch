[project]
name = "SymbolSearch"
version = "0.1.0"
description = "Unicode symbol search and copy tool"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pyside6>=6.11.1",
]

[dependency-groups]
dev = [
    "nuitka>=4.1.2",
]
